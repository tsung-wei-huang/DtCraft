#include <dtc/dtc.hpp>

int main(int argc, char* argv[]) {
  
  std::cout << "This is a demo program\n"
            << "Use the following instructions to create your app:\n"
            << "1. create a folder under 'app/', the name will be your app name\n"
            << "2. run './Makefile.sh' to include your app to our build system\n"
            << "3. type 'make' to build\n";

  return 0;
}
